var normalize = {
    normalize: function(name){
        switch(name){
            case 'piter Filter': 
            name = 'Pitcher Filter';
            break;
            default:
            name = 'Match not Found';
            break;
        }
    }
}